<div class="table-responsive">
    <table class="table table-bordered table-hover table-striped tablesorter">
        <thead>
            <tr>
                <th class="header">Column Family</th>
                <th class="header">Column Qualifier</th>
                <th class="header">Value</th>
            </tr>
        </thead>
        <tbody>
            <% rows.forEach(function(row) { %>
                <tr>
                    <td colspan="3" class="warning"><%= row.key %></td>
                </tr>
                <% Object.keys(row.columnFamilies).forEach(function(columnFamilyKey, columnFamilyIndex) { %>
                    <% var columnFamily = row.columnFamilies[columnFamilyKey]; %>
                    <% Object.keys(columnFamily.columns).forEach(function(columnKey, columnIndex) { %>
                        <% var columnValue = columnFamily.columns[columnKey]; %>
                        <tr>
                            <% if(columnIndex == 0) { %>
                                <td rowspan="<%= Object.keys(columnFamily.columns).length %>"><%= columnFamilyKey %></td>
                            <% } %>
                            <td><%= columnKey %></td>
                            <td><%= columnValue %></td>
                        </tr>
                    <% }) %>
                <% }) %>
            <% }) %>
        </tbody>
    </table>
</div>
